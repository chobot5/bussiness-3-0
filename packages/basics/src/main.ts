import { concatString, redaclared, sum, business30 } from './01-variables'

const renderJson = () => {
  return {
    sum,
    concatString,
    redaclared,
    businessPerson: business30,
  }
}

export default renderJson
