import { concatString, redaclared, sum, business30, person } from './variables'

const variables = {
  sum,
  concatString,
  redaclared,
  businessPerson: business30,
  person,
}

export default variables
